package processing

object TweetProcessor {
  def process(tweet: String): Unit = {
    println(s"Processing tweet: $tweet")

    // Example: Extract hashtags

    // Example: Perform sentiment analysis

    // Add logic to save data to a database, if necessary
  }
}